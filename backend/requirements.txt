# 核心依赖
fastapi>=0.100.0
uvicorn>=0.30.0
sqlalchemy>=2.0.36
redis>=5.2.1
apscheduler>=3.11.0
pydantic>=2.11.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.1
httpx>=0.27.0,<1.0.0  # HTTP 客户端（固定稳定版本）
pydash>=7.4.3
PyJWT>=2.8.0
psycopg2-binary>=2.9.9  # PostgreSQL 驱动
aiomysql>=0.2.0  # 异步 MySQL 驱动（如果需要）
aiosqlite>=0.20.0  # 异步 SQLite 驱动
aiosmtplib>=3.0.0  # 异步 SMTP 邮件发送
jieba>=0.42.1  # 中文分词 (备用)
snownlp>=0.12.3  # 中文情感分析 (备用)
hanlp>=2.1.0  # 中文自然语言处理 - 替代 jieba
cmemotion>=1.0.0  # 中文情感分析 - 替代 snownlp
playwright>=1.40.0,<2.0.0  # 浏览器自动化（固定版本）
feedparser>=6.0.0  # RSS/Atom 解析器

# 测试依赖
pytest>=8.3.5
pytest-asyncio>=0.24.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
